// tuples used for keep data in specific order


let tuser: [string, number, boolean] 

tuser = ["fg", 131, true]

let rgb: [number, number, number] = [255, 255, 255]

type User = [number, string]

const newUser: User = [122, "y@y.com"]

newUser[1] = "yt@y.com"

newUser.push()